node_modules/
dist/
.playwright-cli/
output/
*.tsbuildinfo

# Секреты: токен Telegram-бота и всё, что рядом
.env
.env.local

# Список подписчиков бота — персональные данные
bot/subscribers.json

# Сборки desktop-приложения и их бэкапы
release/
build/

# Локальные настройки инструментов разработки. Исключение — settings.json:
# он выключает трейлеры Co-Authored-By и должен ехать вместе с репозиторием.
# Шаблон именно .claude/* — при исключённом каталоге вернуть файл внутри нельзя.
.claude/*
!.claude/settings.json
